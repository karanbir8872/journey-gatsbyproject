import React from 'react';
import styled from 'styled-components';

const Section = styled.div``;
const Visit = () => (
  <Section className="section">
    <div className="container">
    <div className="columns has-text-centered ">
  <div className="column is-two-fifths">
      <h1 className="has-text-centered is-size-1 has-text-weight-semibold">
        Top Places to visit
      </h1>
      <p className="has-text-centered">Waters make fish every without firmament saw had. Morning air subdue. Our. Air very one. Whales grass is fish whales winged.</p>
      </div>
  </div>
    </div>
  </Section>
);
export default Visit;
